server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://mongo:27017/webserialdb

app:
  mqtt:
    broker: tcp://mosquitto:1883
    clientId: web-serial-server
    topic: device/+/serial/raw
